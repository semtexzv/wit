fn main() -> Result<(), Box<dyn std::error::Error>> {
    /*
    tonic_build::configure()
        .out_dir("./src/proto")
        .compile(
            &[
                //"./proto/router.proto",
                "./proto/control.proto",
                //"./proto/worker.proto"
            ],
            &["../proto"])?;

     */
    Ok(())
}